1) read buffer [X]
2) if readc <= 0, check last (we have to free the whole stash and return the line without the \n)
3) else continuously update the stash with the read value and return the line to \n
